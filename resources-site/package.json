{
  "name": "@fylos/resources-site",
  "version": "1.0.0",
  "description": "Typed, statically generated resources / SEO frontend for Fylos (articles, categories, product updates, company announcements). No backend, no CMS. Content is local typed data, ready to be swapped for a backend or CMS later.",
  "type": "module",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "tsx src/build.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "preview": "tsx src/serve.ts",
    "dev": "tsx src/build.ts && tsx src/serve.ts",
    "check": "npm run lint && npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^20.14.2",
    "eslint": "^9.17.0",
    "globals": "^15.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.19.0"
  }
}
